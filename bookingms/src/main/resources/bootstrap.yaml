server:
  port: 8081

spring:
  application:
    name: booking-ms
  profiles:
    active: default, dev

  cloud:
    config:
      uri: http://localhost:8888
      profile: default

## Actuator
### $ curl localhost:8080/actuator/refresh -d {} -H "Content-Type: application/json"
management:
  endpoints:
    web:
      exposure:
        include: '*'
